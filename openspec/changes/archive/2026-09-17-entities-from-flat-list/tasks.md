# Tasks

- [x] `load_entities` собирает поддерево из `/v4/api/entity` по `parentId`, обход в ширину
      с детьми по возрастанию id — `ctl_worker/ctl_loader.py`
- [x] Заметка о полноте: размер поддерева и число сущностей семейства вне его
- [x] `load_events` читает `ctl_entities_all` из S3 с запасным запросом к CTL
- [x] `ctl_obj_save` пишет Variable и при совпадении MD5 (`_var_set`) — `plugins/ctl_utils.py`
- [x] Стенд 17.09.2026:
      - боевой список альфы (399 804 записи) через новую функцию — поддерево 682 за 0,63 с,
        обе пропавшие сущности на месте;
      - на данных стенда новый сбор совпал со старым обходом дерева: 680, расхождений 0;
      - `tasks test load_entities` — заметка «🌳 поддерево 941010000: 680; семейство 9410…:
        681, вне поддерева 1», объект сохранён;
      - `tasks test load_events` — «Объект ctl_entities_all успешно загружен из S3», второго
        запроса к CTL нет;
      - подменённая переменная после `ctl_obj_save` с тем же набором восстановилась
        (до правки оставалась подменённой)
- [x] Альфа 17.09.2026: после выкладки в `ctl_entity.yml` 682 сущности — ровно размер
      поддерева `941010000` по плоскому списку, то есть `941010665 ub.prod_map` и
      `941010666 ub.market_monitor` доехали

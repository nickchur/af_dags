# Tasks

- [x] `sla_notes` несёт возраст нарушения, вывод разделён на «🚨 SLA» и «🪦 Брошены дольше
      <срока>»: счёт в заголовке, старейшие первыми, «и ещё N», полный список в логе —
      `ctl_worker/ctl_monitor.py`
- [x] Срок `sla_dead` из конфигурации CTL, по умолчанию `days=30` — `ctl_worker/ctl_monitor.py`
- [x] Раздел в `ctl_worker/readme.md`
- [x] Стенд 17.09.2026: даг разбирается (`dags list-import-errors` пусто, `CTL.HR_Data.monitor`
      в списке); отрисовка проверена прогоном логики на 144 нарушениях — было 6370 символов,
      в заметку влезало 25 строк из 144 в порядке id, стало две заметки со счётом и
      старейшими первыми
- [ ] Альфа: сверить на живых данных, что свежие нарушения видны в заметке прогона, а
      брошенные не мешают читать `Action` и `Status`

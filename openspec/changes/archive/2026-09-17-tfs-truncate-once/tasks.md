# Tasks

- [x] `run_tfs`: множество усечённых таблиц, усечение только первому файлу каждой таблицы,
      отметка после успеха — `ctl_worker/ctl_worker.py`
- [x] Раздел в `ctl_worker/readme.md`
- [x] Проверка логики на четырёх сценариях (общая таблица, таблица из имени, без усечения,
      падение первого файла) и разбор дагов на стенде 17.09.2026: ошибок импорта нет, 27 дагов CTL
- [x] Альфа 17.09.2026: сверять нечего — воркфлоу с приёмом ТФС на контуре нет вовсе, тракт
      приёма в разработке. Значит правка (как и близнец внутри архива из #63) сняла мину до
      первого прохода, а не чинила потерю данных: искать недогруженные пачки в `dia` не нужно
- [ ] Первый боевой воркфлоу с `wf_tfs_table` и `wf_tfs_truncate=true`: сверить, что в
      таблице все файлы прогона, а не последний

## 1. Загрузчик

- [ ] 1.1 `load_events`: отсев ключей по `/v4/api/entity` и `/v4/api/profile`, заметка с
      таблицей workflow → ссылка → причина, предохранители (свой профиль, четверть ключей);
      проверено на стенде: висячие ключи из эмулятора не попадают в `ctl_events`
- [ ] 1.2 «Без изменений» — только в конце, по трём объектам; проверено на стенде: второй
      прогон без изменений уходит в skip, имена сущностей обновляются независимо

## 2. Сенсор событий

- [ ] 2.1 `get_events`: `loading_id: null` — событие без загрузки, сбой ключа — в лог, все
      ключи упали — падение; проверено на стенде: ран `events` без `int(None)`

## 3. Эмулятор CTL

- [ ] 3.1 `ctl_mock.py`: полный список сущностей, `GET /v4/api/profile`, 422 на
      несуществующие профиль и сущность; проверено запросами к эмулятору
- [ ] 3.2 `README.md` эмулятора: новые ручки и переменные окружения

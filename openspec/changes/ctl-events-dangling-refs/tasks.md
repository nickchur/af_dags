## 1. Загрузчик

- [x] 1.1 `load_events`: отсев ключей по `/v4/api/entity` и `/v4/api/profile`, заметка с
      таблицей workflow → ссылка → причина, предохранители (свой профиль, четверть ключей);
      проверено на стенде (эмулятор: нет профиля `arnsdpcc360` и сущности 466163228): `ctl_events`
      17 → 15, заметка с таблицей и workflow 125868, 220389
- [x] 1.2 «Без изменений» — только в конце, по трём объектам; проверено на стенде: первый
      прогон обновил `ctl_enames` и `ctl_entity_events`, второй ушёл в skip по всем трём

## 2. Сенсор событий

- [x] 2.1 `get_events`: `loading_id: null` — событие без загрузки, сбой ключа — в лог, все
      ключи упали — падение; проверено на стенде: до — падение `int(None)` на
      третьей попытке и заметка «Ошибка API»; после — проверки 07:12–07:17 на первой попытке, 9 ключей, без 422

## 3. Эмулятор CTL

- [x] 3.1 `ctl_mock.py`: полный список сущностей, `GET /v4/api/profile`, 422 на
      несуществующие профиль и сущность; проверено запросами к эмулятору
- [x] 3.2 `README.md` эмулятора: новые ручки и переменные окружения

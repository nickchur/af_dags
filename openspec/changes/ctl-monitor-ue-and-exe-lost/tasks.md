# Tasks

## 1. Воркер

- [x] 1.1 `gp_backend_busy(lid)`: запуск ищется регуляркой по `"lid": <id>` в тексте запроса, свой
      процесс исключён; `run_exe` ставит `lid` первым полем JSON.
- [x] 1.2 `ctl_exe_recover`: исходы `ok` / `lost` / `fail`, журнал перепроверяется перед `lost`.
- [x] 1.3 `run_exe`: на `lost` — `result` с `res = -9`, таск успешен; вызов процедуры одной попыткой.
- [x] 1.4 `run_end`: `priority_weight = 900`.
- [x] 1.5 `_wf_eligible` и счётчик загрузчика: профиль контура и `dummy`.

## 2. Монитор и загрузчик

- [x] 2.1 Ветка своих: `profile_ids` в запросе загрузок.
- [x] 2.2 Ветка UE: `ue_action`, пороги в `ctl_config`, `Started` для сорвавшегося расписания.
- [x] 2.3 Загрузчик: `orphan` / `double` в заметке и в `ctl_workflows_stat`.
- [x] 2.4 Попутно: `ABORTING` падал на `log.split(' ').strip()`.

## 3. Проверка

- [ ] 3.1 Стенд (AF2 по команде), эмулятор CTL: обрыв на первой попытке → `res -9` и повтор в CTL;
      живой запуск → ожидание и ответ.
- [ ] 3.2 Стенд: UE-загрузки — зависшая, долгая, сорвавшееся расписание.
- [ ] 3.3 Альфа после выкладки: заметка загрузчика о потоках вне присмотра.

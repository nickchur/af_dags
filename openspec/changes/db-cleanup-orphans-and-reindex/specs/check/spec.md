## ADDED Requirements

### Requirement: Результаты celery без даты завершения тоже вычищаются

Очистка `celery_taskmeta` SHALL удалять и строки, у которых даты завершения нет вовсе:
возраст такой строки SHALL определяться по её возрастающему первичному ключу, а граница —
как максимум ключа среди строк, уже вышедших за срок хранения. Строка работающей задачи
SHALL оставаться нетронутой. Когда ни одна строка за срок хранения не вышла, не SHALL
удаляться ничего.

#### Scenario: Воркер умер, не закрыв задачу

- **WHEN** в таблице лежит строка в состоянии `STARTED` без даты завершения, появившаяся
  раньше границы срока хранения
- **THEN** она попадает под удаление — в отличие от прежнего условия по одной только дате,
  под которое она не попадала никогда

#### Scenario: Задача работает прямо сейчас

- **WHEN** строка в состоянии `STARTED` без даты завершения появилась позже границы
- **THEN** она не удаляется

#### Scenario: В таблице нет ничего старого

- **WHEN** ни одна строка не вышла за срок хранения
- **THEN** граница не определена и не удаляется ни одна строка без даты

### Requirement: Переиндексация идёт по очищенным таблицам и включается ключом переменной

Переиндексация SHALL выполняться по тем таблицам, которые вернула очистка, а не по схеме
целиком, с отдельным таймаутом на таблицу и строкой отчёта на каждую. Пропуск по правам и
отказ SHALL попадать в строку своей таблицы и не прерывать остальные.

Параметр переиндексации SHALL отсутствовать в форме запуска и SHALL NOT создавать задачу,
пока в сохранённых параметрах нет соответствующего ключа. Сохранённые параметры SHALL нести
версию набора, и при её смене устаревшие ключи SHALL выбрасываться поодиночке, а остальные
значения — сохраняться.

#### Scenario: Очистка ничего не удаляла

- **WHEN** очистка вернула пустой список таблиц
- **THEN** переиндексация пропускается: раздувает индексы массовое удаление, а его не было

#### Scenario: Ключа переиндексации в переменной нет

- **WHEN** в сохранённых параметрах нет ключа переиндексации
- **THEN** ни параметра в форме, ни задачи в графе нет, а цепочка задач остаётся связной

#### Scenario: Переход на новую версию набора параметров

- **WHEN** в переменной лежит набор прежней версии с ключом переиндексации
- **THEN** ключ выбрасывается, расписание и прочие значения остаются, версия проставляется;
  повторный запуск переменную не трогает

## Происхождение

- **Возраст по ключу, а не по дате завершения.** Дата появляется только при переходе задачи
  в терминальное состояние, а у сироты такого перехода не было. Замер на стенде 18.09.2026:
  дата стоит у всех 314 771 `SUCCESS` и 49 `FAILURE` и отсутствует у обеих `STARTED`; на
  альфе осадка накопился 91. Пересматривать, если celery начнёт заполнять дату при старте.
- **Граница — максимум ключа среди старых, а не минимум среди свежих.** Задача, начатая 40
  дней назад и закончившаяся 5 дней назад, имеет маленький ключ и свежую дату: она утянула
  бы минимум вниз и застопорила чистку.
- **Переиндексация по таблицам.** `REINDEX SCHEMA CONCURRENTLY` проходил все индексы схемы
  одной командой на час без обратной связи, включая таблицы, которых чистка не касается.
- **Версия набора параметров.** Сохранение пишет форму целиком, поэтому у всех, кто когда-либо
  сохранял параметры, ключ переиндексации остался бы в переменной и фича осталась бы
  включённой именно у тех, кто DAG-ом пользуется. Удалять переменную целиком нельзя: вместе
  с ключом уехало бы расписание и вернулось к значению из кода.

# Задачи

## Сироты celery

- [x] `_BASE_WHERE` — точечная замена условия возраста там, где `<колонка> < :cutoff` мало
      (нужен OR, а `_EXTRA_COND` умеет только AND)
- [x] Граница по `id`: максимум среди уже старых, `NULL` при их отсутствии не удаляет ничего
- [x] Комментарий про трёхзначную логику: у живой строки условие именно `NULL`, и «чинить»
      это через `COALESCE` нельзя
- [x] Проверено на стенде откатом: подделанная из существующей строки старая сирота
      удаляется (было — нет), обе живые `STARTED` остаются

## Реиндекс — убран целиком (21.09.2026)

- [x] Промежуточный вариант (по таблицам, скрыт ключом, миграция `params_version`,
      `store_params(extra=...)`) снят решением пользователя
- [x] Удалены параметр, таск и `db_reindex`, миграция и `_DROP_ON_MIGRATE`; `save_params`
      вернулся к виду master; `plugins/utils.py` и `plugins/readme.md` не меняются
- [x] Цепочка `params → clean → vacuum → report`

## Документы

- [x] Строка версии `db_cleanup.py` v1.11, `tools/readme.md` v1.17
- [x] Таблица параметров и список тасков в docstring
- [ ] Прогон на боевой метабазе: сверить, что осадок сирот ушёл, а живые задачи не тронуты

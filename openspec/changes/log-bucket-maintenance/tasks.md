## 1. Даг

- [x] 1.1 Параметры: `days` для логов задач, `folders` (префикс → дни), `other_days`,
      `lifecycle`; значения по умолчанию из `tools_log_cleanup_params`
- [x] 1.2 Таск `layout`: бакет и правила жизненного цикла на префиксы; отказ правила не
      роняет прогон, проблемные папки — в заметку
- [x] 1.3 Таск `sweep`: один обход, статистика по папкам и удаление старше своего срока
      партиями по 1000 ключей
- [x] 1.4 Таск `report`: таблица по папкам в заметку таска и DAG-а, сводка в XCom
- [x] 1.5 `dry_run`: ничего не удаляем и правил не меняем, отчёт показывает, что удалили бы.
      Нужен потому, что правило жизненного цикла работает само: уменьшение срока удаляет
      данные асинхронно, без обхода
- [x] 1.6 `max_minutes`: потолок обхода, отчёт честно говорит, что данные неполные
- [x] 1.7 Докстринг v1.7 и `check/readme.md` приведены к коду (в описании были параметры
      `months` и не было таска `params`)

## 2. Проверка на стенде

- [x] 2.1 Даг разбирается, таски `params → layout → sweep → report`, параметры на месте
- [x] 2.2 Прогон против MinIO: правила выставлены, обходом удалено 325 объектов старше
      12 дней, таблица по папкам сошлась (`airflow-logs/`, `dag_snapshots/`,
      `airflow-logs/_health/`, `queue_cleanup/`)
- [x] 2.3 Правила на MinIO **работают**: прогон с `days=7` поставил правило, и хранилище само
      убрало 23,5 тыс. объектов между прогонами — отсюда `dry_run` и предупреждение в описании
- [x] 2.4 Сухой прогон: удалено 0, в отчёте «объектов старше срока при обходе: 14923»
- [x] 2.5 Потолок обхода: при `max_minutes=4` отчёт сказал, что обход не закончен
- [x] 2.6 Папки без срока не осматриваются, отчёт об этом говорит (`other_days=0`)
- [x] 2.7 Ловушки проверены по всем писателям в бакет: `dag_snapshots/` (версии живы, пока жив
      даг) и `tfs/` (очередь на отправку) получили срок 0 — уборка по возрасту снесла бы нужное
- [x] 2.8 Перепись папок листингом с разделителем: отчёт назвал `demo/` и `er_dump/`, которых
      нет в карте

## 3. Выкладка

- [ ] 3.1 На альфе проверить, применяются ли правила жизненного цикла: в отчёте не должно
      оставаться объектов старше срока в папках, где правило стоит

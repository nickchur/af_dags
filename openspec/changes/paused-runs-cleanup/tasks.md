# Задачи

- [x] `tools/paused_runs_cleanup.py`: `classify`, даг `tools_paused_runs_cleanup`
- [x] Стенд 24.09.2026, временный sqlite: отчёт по порогу 24 ч и 1 ч, ран не на паузе не
      тронут, задача в running защищает ран, `max_runs=0`, закрытие через Mark failed и
      запросом (без сериализации), заметка и событие журнала, битое и пустое расписание
- [x] `tools/readme.md`
- [ ] После выкладки: запуск без `close` на сигме и альфе, сверка с `runs.paused_active`

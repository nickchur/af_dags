# Tasks

- [x] `add_note`: перечитывание строки под `FOR UPDATE`, стабильный порядок уровней,
      мягкий откат при неудаче блокировки — `plugins/utils.py`
- [x] `gp_upload_s3_csv`: усечение один раз на объект S3 — `plugins/ctl_utils.py`
- [x] `s3_IterStream.read(-1)`: набор по чанку с проверкой порога — `plugins/s3_utils.py`
- [x] `eval_delta`: `ValueError` с текстом куска и дельты, пустое значение пропускается —
      `plugins/ctl_utils.py`
- [x] Стенд 17.09.2026:
      - гонка заметок, 8 процессов в один ран: старый код — 4 строки из 8, новый — 8 из 8
        (дважды подряд);
      - ZIP из двух файлов: вызовы загрузчика `[(12, True), (8, False)]`, без усечения —
        оба `False`;
      - `read(-1)`: короткий поток цел, поток на 600 МБ упёрся в `MemoryError` на пороге
        (пик 593 МБ), порционное чтение не изменилось;
      - `eval_delta`: рабочие формы считаются как раньше, `hours 5` и `hours` дают
        `ValueError` с текстом, `hours=` и лишняя запятая пропускаются
- [ ] Альфа: после выкладки убедиться, что заметка `ctl_loader` содержит строки всех задач

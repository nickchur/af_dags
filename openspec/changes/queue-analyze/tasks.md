# Задачи

- [x] `tools/queue_analyze.py`: `scheduler_causes`, `conclusions`, таски `scheduler`, `capacity`,
      `report`; разметка и чистка брокера перенесены из `queue_cleanup.py` без изменений
- [x] Стенд 24.09.2026, временный sqlite: лимит дага, пул, приоритет, ран без старта, запаузенный
      даг — каждый назван в выводах; свой ран в разбор не попадает
- [ ] Разметка брокера на redis стенда (`purge=False` / `purge=True`) — код перенесён без
      изменений, прогоном не подтверждён
- [ ] После выкладки: запуск на сигме, сверка с `platform.scheduled` в health

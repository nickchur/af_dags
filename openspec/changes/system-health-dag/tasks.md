# Tasks

- [x] DAG `tools_system_health`, семь проверок, итог в лог, XCom и заметку — `check/system_health.py`
- [x] Порог отставшего файла относительный: на стенде круг разбора (~160 с) дольше
      `min_file_process_interval + dag_file_processor_timeout` (80 с) — `check/system_health.py`
- [x] Раздел в `check/readme.md`
- [x] Стенд, 14.09.2026:
  - разбор файла без ошибок;
  - ручной прогон — success за 12,8 с, `warn` от застрявшего `scheduled`;
  - несуществующий бакет — `error` за 0,4 с;
  - неотвечающий S3 — `error` за 5 с;
  - закрытый брокер — `error` без пароля в причине;
  - `tasks test` с закрытым брокером — `AirflowFailException`, заметка записана;
  - подсаженный битый файл — назван среди новых ошибок импорта
- [ ] Альфа: сутки прогонов раз в час — время прогона, не шумит ли `pools` (динамические
      пулы ctl), порог доставки
